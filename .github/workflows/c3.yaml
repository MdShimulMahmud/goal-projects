name: Conditional Workflow (Third Workflow)

on:
  workflow_run:
    workflows: ["Approval Workflow"]
    types:
      - completed

jobs:
  deploy:
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}  # Runs only if Approval Workflow is successful

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Read approval status
        run: |
          echo "Approval Status: ${{ env.APPROVED }}"
        
      - name: Conditional Deployment
        if: ${{ env.APPROVED == 'true' }}  # Run deployment only if approved
        run: |
          echo "Deploying to production..."
          # Add deployment commands (e.g., AWS CLI, Docker, Kubernetes, etc.)
